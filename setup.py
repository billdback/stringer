from setuptools import setup, find_packages

setup(name='stringer',
      version='0.1',
      description='This package is a test package for structuring packages.',
      author='William D Back',
      author_email='billback@mac.com',
      license='MIT',
      packages=find_packages(),
      zip_safe=False
      )
